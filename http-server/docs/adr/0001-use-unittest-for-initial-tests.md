# Use `unittest` for the initial test suite

The project uses Python's built-in `unittest` framework for its initial tests instead of `pytest`. This keeps the small learning project dependency-free and runnable with the standard Python installation; `pytest` can be reconsidered if the test suite grows enough to benefit from its simpler syntax and fixtures.
